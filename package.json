{
  "name": "roanixs-api",
  "version": "0.0.3",
  "description": "Simple wrapper / configuration module for interacting with APIs ",
  "main": "dist/index.js",
  "scripts": {
    "docs": "esdoc -c esdoc.json",
    "dev": "babel --optional runtime --modules common src --watch --out-file dist/index.js",
    "compile": "babel --optional runtime --modules common src --out-file dist/index.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "fetch",
    "es6"
  ],
  "author": "Joani COUMA",
  "license": "ISC",
  "dependencies": {
    "whatwg-fetch": "^0.9.0"
  },
  "devDependencies": {
    "babel": "^5.8.23",
    "babel-runtime": "^5.8.25"
  }
}
